# 🐾 Feed API 테스트 시나리오

## 📋 개요
이 문서는 Feed 모듈의 모든 API 엔드포인트에 대한 테스트 시나리오를 제공합니다.

## 🎯 테스트 목표
- 모든 API 엔드포인트의 정상 동작 확인
- 에러 케이스 처리 검증
- 데이터 검증 및 응답 형식 확인
- 인증 및 권한 검증

## 🚀 테스트 환경 설정

### 1. 서버 실행
```bash
cd final-team2-backend
./gradlew bootRun
```

### 2. 테스트 페이지 접속
```
http://localhost:8080/test-feed-api.html
```

## 📝 API 테스트 시나리오

### 1. 피드 목록 조회 API (`GET /api/feed`)

#### 1.1 기본 조회 테스트
**시나리오**: 파라미터 없이 기본 피드 목록 조회
- **요청**: `GET /api/feed`
- **예상 결과**: 
  - HTTP 200 OK
  - 페이징된 피드 목록 반환
  - 기본 정렬: 최신순
  - 기본 페이지 크기: 6

#### 1.2 검색 조건 테스트
**시나리오**: 제목으로 피드 검색
- **요청**: `GET /api/feed?title=제주`
- **예상 결과**: 제목에 "제주"가 포함된 피드만 반환

**시나리오**: 작성자로 피드 검색
- **요청**: `GET /api/feed?author=김여행`
- **예상 결과**: 작성자가 "김여행"인 피드만 반환

**시나리오**: 지역으로 피드 검색
- **요청**: `GET /api/feed?region=JEJU`
- **예상 결과**: 제주 지역 피드만 반환

#### 1.3 정렬 테스트
**시나리오**: 조회순 정렬
- **요청**: `GET /api/feed?sort=view`
- **예상 결과**: 조회수가 높은 순으로 정렬

**시나리오**: 좋아요순 정렬
- **요청**: `GET /api/feed?sort=like`
- **예상 결과**: 좋아요수가 높은 순으로 정렬

#### 1.4 페이징 테스트
**시나리오**: 페이지 크기 변경
- **요청**: `GET /api/feed?size=10`
- **예상 결과**: 페이지당 10개 피드 반환

**시나리오**: 특정 페이지 조회
- **요청**: `GET /api/feed?page=1`
- **예상 결과**: 두 번째 페이지 피드 반환

### 2. 피드 생성 API (`POST /api/feed`)

#### 2.1 정상 생성 테스트
**시나리오**: 필수 필드만으로 피드 생성
- **요청**:
```json
{
  "title": "제주도 여행 후기",
  "content": "제주도에서 멋진 시간을 보냈습니다."
}
```
- **예상 결과**: 
  - HTTP 200 OK
  - 생성된 피드 정보 반환
  - 자동으로 현재 시간이 생성 시간으로 설정

#### 2.2 모든 필드 포함 생성 테스트
**시나리오**: 모든 필드를 포함하여 피드 생성
- **요청**:
```json
{
  "title": "부산 해운대 여행",
  "content": "부산 해운대에서 일몰을 보며 힐링했습니다.",
  "imageUrl": "https://example.com/image.jpg",
  "location": "BUSAN",
  "badgeRequest": true
}
```
- **예상 결과**: 모든 필드가 정상적으로 저장된 피드 반환

#### 2.3 유효성 검증 테스트
**시나리오**: 제목 누락
- **요청**:
```json
{
  "content": "내용만 있는 피드"
}
```
- **예상 결과**: HTTP 400 Bad Request, 제목 필수 에러

**시나리오**: 내용 누락
- **요청**:
```json
{
  "title": "제목만 있는 피드"
}
```
- **예상 결과**: HTTP 400 Bad Request, 내용 필수 에러

**시나리오**: 잘못된 지역 값
- **요청**:
```json
{
  "title": "테스트",
  "content": "테스트",
  "location": "INVALID_LOCATION"
}
```
- **예상 결과**: HTTP 400 Bad Request, 잘못된 지역 값 에러

### 3. 피드 상세 조회 API (`GET /api/feed/{feedId}`)

#### 3.1 정상 조회 테스트
**시나리오**: 존재하는 피드 상세 조회
- **요청**: `GET /api/feed/1`
- **예상 결과**:
  - HTTP 200 OK
  - 피드 상세 정보 반환
  - 조회수 1 증가
  - 댓글 목록 포함

#### 3.2 존재하지 않는 피드 조회 테스트
**시나리오**: 존재하지 않는 피드 ID로 조회
- **요청**: `GET /api/feed/99999`
- **예상 결과**: HTTP 404 Not Found, 피드를 찾을 수 없음 에러

### 4. 피드 수정 API (`PUT /api/feed/{feedId}`)

#### 4.1 정상 수정 테스트
**시나리오**: 작성자가 자신의 피드 수정
- **요청**:
```json
{
  "title": "수정된 제목",
  "content": "수정된 내용",
  "location": "SEOUL"
}
```
- **예상 결과**: 
  - HTTP 200 OK
  - 수정된 피드 정보 반환
  - 수정 시간 업데이트

#### 4.2 권한 검증 테스트
**시나리오**: 다른 사용자가 피드 수정 시도
- **요청**: 다른 사용자로 로그인하여 피드 수정
- **예상 결과**: HTTP 403 Forbidden, 권한 없음 에러

#### 4.3 존재하지 않는 피드 수정 테스트
**시나리오**: 존재하지 않는 피드 ID로 수정 시도
- **요청**: `PUT /api/feed/99999`
- **예상 결과**: HTTP 404 Not Found, 피드를 찾을 수 없음 에러

### 5. 피드 삭제 API (`DELETE /api/feed/{feedId}`)

#### 5.1 정상 삭제 테스트
**시나리오**: 작성자가 자신의 피드 삭제
- **요청**: `DELETE /api/feed/1`
- **예상 결과**: 
  - HTTP 200 OK
  - "피드가 삭제되었습니다." 메시지 반환

#### 5.2 권한 검증 테스트
**시나리오**: 다른 사용자가 피드 삭제 시도
- **요청**: 다른 사용자로 로그인하여 피드 삭제
- **예상 결과**: HTTP 403 Forbidden, 권한 없음 에러

### 6. 댓글 생성 API (`POST /api/feed/{feedId}/comments`)

#### 6.1 정상 댓글 생성 테스트
**시나리오**: 피드에 댓글 작성
- **요청**:
```json
{
  "content": "멋진 여행 후기네요!"
}
```
- **예상 결과**:
  - HTTP 200 OK
  - 생성된 댓글 정보 반환
  - 작성자 정보 포함

#### 6.2 댓글 내용 검증 테스트
**시나리오**: 빈 댓글 작성 시도
- **요청**:
```json
{
  "content": ""
}
```
- **예상 결과**: HTTP 400 Bad Request, 댓글 내용 필수 에러

### 7. 댓글 목록 조회 API (`GET /api/feed/{feedId}/comments`)

#### 7.1 정상 댓글 목록 조회 테스트
**시나리오**: 피드의 댓글 목록 조회
- **요청**: `GET /api/feed/1/comments`
- **예상 결과**:
  - HTTP 200 OK
  - 댓글 목록 반환 (최신순 정렬)
  - 각 댓글에 작성자 정보 포함

## 🔍 데이터 검증 체크리스트

### 피드 데이터 검증
- [ ] 피드 ID가 올바르게 생성되는지
- [ ] 작성자 정보가 정확한지
- [ ] 생성 시간이 현재 시간으로 설정되는지
- [ ] 수정 시간이 업데이트되는지
- [ ] 조회수가 정확히 증가하는지
- [ ] 좋아요 수가 정확한지
- [ ] 지역 정보가 올바른 enum 값인지

### 댓글 데이터 검증
- [ ] 댓글 ID가 올바르게 생성되는지
- [ ] 댓글 작성자 정보가 정확한지
- [ ] 댓글이 해당 피드에 올바르게 연결되는지
- [ ] 댓글 목록이 시간순으로 정렬되는지

### 페이징 검증
- [ ] 페이지 크기가 올바르게 적용되는지
- [ ] 총 페이지 수가 정확한지
- [ ] 총 요소 수가 정확한지
- [ ] 페이지 번호가 올바른지

## 🚨 에러 케이스 테스트

### 400 Bad Request
- [ ] 필수 필드 누락
- [ ] 잘못된 데이터 형식
- [ ] 유효하지 않은 enum 값

### 401 Unauthorized
- [ ] 인증되지 않은 요청
- [ ] 만료된 토큰

### 403 Forbidden
- [ ] 다른 사용자의 피드 수정/삭제
- [ ] 다른 사용자의 댓글 수정/삭제

### 404 Not Found
- [ ] 존재하지 않는 피드 조회/수정/삭제
- [ ] 존재하지 않는 댓글 수정/삭제

### 500 Internal Server Error
- [ ] 데이터베이스 연결 오류
- [ ] 서버 내부 오류

## 📊 성능 테스트

### 부하 테스트
- [ ] 대량의 피드 생성 시 성능
- [ ] 대량의 댓글 생성 시 성능
- [ ] 복잡한 검색 조건 시 성능

### 동시성 테스트
- [ ] 동시에 같은 피드 조회
- [ ] 동시에 같은 피드에 댓글 작성
- [ ] 동시에 같은 피드 수정

## 🎯 테스트 완료 기준

### 기능 테스트
- [ ] 모든 API 엔드포인트가 정상 동작
- [ ] 모든 에러 케이스가 올바르게 처리
- [ ] 데이터 검증이 정확히 수행
- [ ] 권한 검증이 올바르게 동작

### 성능 테스트
- [ ] API 응답 시간이 1초 이내
- [ ] 동시 요청 처리가 안정적
- [ ] 메모리 사용량이 적절

### 보안 테스트
- [ ] 인증되지 않은 접근 차단
- [ ] 권한 없는 작업 차단
- [ ] SQL Injection 방지
- [ ] XSS 공격 방지

## 📝 테스트 결과 기록

테스트 실행 후 다음 정보를 기록하세요:

1. **테스트 일시**: YYYY-MM-DD HH:MM
2. **테스트 환경**: 개발/스테이징/프로덕션
3. **테스트 결과**: 성공/실패
4. **발견된 이슈**: 
5. **개선 사항**:
6. **테스터**: 

---

**참고**: 이 테스트 시나리오는 지속적으로 업데이트되어야 하며, 새로운 기능이나 변경사항이 있을 때마다 검토하고 수정해야 합니다. 

4. 기능 테스트 체크리스트
4-1. ProfileAnswer API 테스트
[ ] 선호도 생성 (POST /api/profile-answers)
[ ] 선호도 수정 (PUT /api/profile-answers)
[ ] 선호도 저장/수정 (POST /api/profile-answers/save-or-update)
[ ] 선호도 조회 (GET /api/profile-answers/my)
[ ] 선호도 존재 확인 (GET /api/profile-answers/my/exists)
[ ] 선호도 삭제 (DELETE /api/profile-answers)
4-2. Recommendation API 테스트
[ ] 유사도 계산 (GET /api/recommendation/similarity)
[ ] 유사한 사용자 검색 (GET /api/recommendation/similar-users)
[ ] 임베딩 생성/업데이트 (POST /api/recommendation/embeddings)
[ ] 시스템 상태 확인 (GET /api/recommendation/status)
4-3. 연동 테스트
[ ] ProfileAnswer 생성 시 임베딩 자동 생성
[ ] ProfileAnswer 수정 시 임베딩 자동 업데이트
[ ] ProfileAnswer 삭제 시 임베딩 자동 삭제